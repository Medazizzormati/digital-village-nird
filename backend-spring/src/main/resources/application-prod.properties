# Production Configuration
# Use 'update' to automatically create missing tables on first deployment
# After first successful deployment, you can change to 'validate' for safety
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
logging.level.root=WARN
logging.level.com.nird=INFO

